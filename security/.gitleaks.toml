# Gitleaks configuration (minimal)

[gitleaks]
title = "Gitleaks config for P10"

# Allowlist for known false positives (example)
[allowlist]
description = "Global allowlist"
# Example regex allowlist: dummy tokens used in tests/docs
regexes = ["dummy_token_[0-9]{3}"]
paths = []
commits = []
files = []
