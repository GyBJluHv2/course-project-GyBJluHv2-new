rules:
  - id: avoid-dangerous-eval
    message: "Avoid using eval() on untrusted input; use safe parsing instead."
    severity: ERROR
    languages: [python]
    patterns:
      - pattern: eval(...)
    metadata:
      cwe: "CWE-95"
      category: security
      references:
        - https://cwe.mitre.org/data/definitions/95.html
